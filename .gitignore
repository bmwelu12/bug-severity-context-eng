# data/raw/*.csv and *.npy are tracked via Git LFS (see .gitattributes),
# not ignored. vocab.lst is small enough to commit as a plain text file.
data/processed/*
!data/processed/.gitkeep
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
